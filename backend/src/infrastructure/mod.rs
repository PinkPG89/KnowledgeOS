//! # Infrastructure (인프라스트럭처) 모듈
//!
//! 이 모듈은 도메인과 애플리케이션 계층이 요구하는 기능 중 운영체제(OS), 파일 시스템(I/O),
//! 또는 외부 외부 프로세스와 물리적으로 소통해야 하는 저수준 어댑터(Adapter)들을 구현합니다.
//!
//! ## 인프라 계층의 역할 (Clean Architecture)
//! * **현실 세계와의 물리적 소통**: 도메인 계층(domain)이 선언한 순수한 논리적 규칙(예: CanonicalPath)을
//!   실제 물리적인 디스크 상의 디렉터리 경로와 매핑하고, 운영체제의 파일 읽기/쓰기 시스템 콜을 수행합니다.
//! * **외부 서비스 추상화**: 파일 시스템 제어, 데이터베이스 연결, 원격 API 통신 등 시스템 외부 자원에 대한
//!   구체적인 접근 로직을 한데 모아 관리합니다.

// 마크다운 저장소(Vault)의 물리적 루트 검증 및 실질적인 디바이스 접근 제어를 수행하는 `vault` 모듈을 공개합니다.
pub mod vault;
